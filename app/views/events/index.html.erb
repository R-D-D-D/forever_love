<div class="container-fluid px-5 brick-bg">
  <!-- title -->
  <div class="row py-3">
    <div class="col text-center">
      <h1 class="display-4 text-uppercase text-light">
        <strong>相册</strong>
      </h1>
      <div class="title-underline bg-danger"></div>
    </div>
  </div>
  <!-- end of title -->
  <%= render 'events/carousel' %>
  <!-- title -->
  <div class="row pt-5 justify-content-start">
    <div class="col d-flex justify-content-between align-items-center">
      <h1 class="display-4 text-uppercase text-light ml-3">
        全部
      </h1>
      <%= link_to "创建相册", new_event_path, class: 'nav-link' %>
    </div>
    <div class="title-underline bg-danger full"></div>
  </div>
  <!-- end of title -->
  <div class="row">
    <% if @events.any? %>
        <%= render @events %>
    <% end %>
  </div>
</div>